# testing_dir_options
I will definitely delete this. I'm testing options before I pick one for UCF_CMR

# bash commands:
Add your repo to this repo:
```
git subtree add --prefix=<local-directory> <remote-url> <remote-branch> --squash
```
